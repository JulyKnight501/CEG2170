#include <stdio.h>
#include <stdlib.h>

/*
*   Currently CEG2170 Template
*   See Makefile for what is happening in background for "make run"
*
*   Notes from class:
*   Order of Operation in C
*   Does conditions then &&. ||, !
*
*   ! = if something is true mak it false, and if something is false make it true.
*   
*   syntax error = unused variable, 
*   logic error = give warning with logical statesments does not run as you think it should
*   run-time error = program crashes for some unknow reason
*   
*/

int main () {

    
    char c = while(getchar(!= '\n'));
    printf("%c", c);

    return 0;
}